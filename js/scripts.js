jQuery(document).ready(function($) {
    // $('.price-items .entry').each(function(index, el) {
    //    if( $(this).hasClass('current') ){
    //         $('.price-value').val($(this).data('value'));
    //    }
    // });
    // $('.price-items .entry').each(function(index, el) {
    //    if( $(this).hasClass('current') ){
    //         $('.price-display').html($(this).data('price'));
    //    }
    // });
    // $('.price-items .entry').click(function(event) {
    //     $('.price-items .entry').removeClass('current');
    //     $(this).addClass('current');
    //     $('.price-display').html($(this).data('price'));
    //     $('.price-value').val($(this).data('value'));
    // });
});
